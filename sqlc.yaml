version: "2"
sql:
  - engine: "sqlite"
    queries: "src/repository/queries"
    schema: "src/repository/migrations"
    gen:
      go:
        package: "repository"
        out: "src/repository"
        emit_json_tags: true